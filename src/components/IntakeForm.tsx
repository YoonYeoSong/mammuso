"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import Link from "next/link";
const relationships = ["연인/썸", "친구", "가족", "직장", "기타"] as const;
export function IntakeForm() { const router = useRouter(); const [relationshipType, setRelationshipType] = useState<(typeof relationships)[number]>("친구"); const [statement, setStatement] = useState(""); const [privacyPolicyAgreed, setPrivacyPolicyAgreed] = useState(false); const [aiProcessingAgreed, setAiProcessingAgreed] = useState(false); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); async function submit(event: React.FormEvent) { event.preventDefault(); setError(""); setLoading(true); try { const response = await fetch("/api/cases", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ relationshipType, statement, privacyPolicyAgreed, aiProcessingAgreed }) }); const data = await response.json(); if (!response.ok) throw new Error(data.message); router.push(`/case/${data.applicantToken}`); } catch (e) { setError(e instanceof Error ? e.message : "접수에 실패했습니다."); } finally { setLoading(false); } } return <form className="paper form" onSubmit={submit}><label>관계 유형</label><div className="chips">{relationships.map((type) => <button type="button" className={relationshipType === type ? "chip selected" : "chip"} onClick={() => setRelationshipType(type)} key={type}>{type}</button>)}</div><label htmlFor="statement">어떤 일이 있었나요?</label><textarea id="statement" value={statement} onChange={(e) => setStatement(e.target.value)} minLength={20} maxLength={5000} placeholder="사건의 흐름, 약속이나 연락이 오간 시점, 내가 들은 말 등을 편하게 적어주세요." required /><div className="privacy-guard"><b>입력 전 꼭 확인해주세요</b><p>실명, 연락처, 주소, 주민등록번호, 비밀번호, 제3자의 개인정보, 대화 원문 전체는 적지 마세요. 폭력·성폭력·스토킹·자해 등 즉시 위험한 상황은 이 서비스가 아닌 긴급 도움기관을 먼저 이용해야 합니다.</p></div><label className="consent"><input type="checkbox" checked={privacyPolicyAgreed} onChange={(event) => setPrivacyPolicyAgreed(event.target.checked)} required /> <span><Link href="/privacy" target="_blank">개인정보처리방침</Link>을 읽었으며, 사건 기록의 수집·보관에 동의합니다.</span></label><label className="consent"><input type="checkbox" checked={aiProcessingAgreed} onChange={(event) => setAiProcessingAgreed(event.target.checked)} required /> <span>사건 조정 의견 생성에 필요한 최소 내용이 서버를 거쳐 AI 제공업체에 전달되는 것에 동의합니다.</span></label>{error && <p className="error">{error}</p>}<button className="button full" disabled={loading || !privacyPolicyAgreed || !aiProcessingAgreed}>{loading ? "김햄찌 주무관이 서류를 검토 중입니다…" : "동의하고 민원 접수하기"}</button></form>; }
