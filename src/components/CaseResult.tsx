import type { DecisionResult } from "@/lib/cases/types";
export function CaseResult({ result, rehearing = false }: { result: DecisionResult; rehearing?: boolean }) { return <section className="result-card"><p className="eyebrow">{rehearing ? "재심 민원 처리결과" : "민원 처리결과"}</p><h2>{rehearing ? "재심 조정결과" : "쌍방 진술 종합 조정결과"}</h2><p>{result.overview}</p><div className="ratio"><div style={{ width: `${result.complainantResponsibility}%` }}><b>신청인 {result.complainantResponsibility}</b></div><div><b>상대방 {result.respondentResponsibility}</b></div></div><p className="ratio-caption">관계분쟁 책임지표 · 합계 100</p><ResultList title="양측 인정 사실" items={result.agreedFacts} /><ResultList title="신청인 주장" items={result.complainantClaims} /><ResultList title="상대방 주장" items={result.respondentClaims} /><ResultList title="주요 쟁점" items={result.disputedFacts} /><ResultList title="판단불가 사항" items={result.unknownFacts} /><section><h3>맘무소 조정의견</h3><p>{result.reasoning}</p><p className="advice">{result.mediationAdvice}</p></section>{result.changedReason && <section><h3>변경 이유</h3><p>{result.changedReason}</p></section>}<div className="clerk-note">🐹 김햄찌 주무관 의견: {result.clerkComment}</div><small>본 수치는 양측이 제출한 진술을 바탕으로 생성한 맘무소 내부 참고용 관계분쟁 조정지표이며 법률적 과실비율이나 법적 판단이 아닙니다.</small></section>; }
function ResultList({ title, items }: { title: string; items: string[] }) { return <section><h3>{title}</h3>{items.length ? <ul>{items.map((item, index) => <li key={index}>{item}</li>)}</ul> : <p>확인되지 않음</p>}</section>; }
